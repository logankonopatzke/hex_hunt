#pragma once

/*
Configuration options for hex hunt
*/

#define MIN_HEX_ENTITY_SPAWN_DISTANCE 3 // The minimum distance that hex entities will spawn away from the cursor
#define NUMBER_OF_HEX_ENTITIES 5        // The number of hex entities to spawn in a round
#define SCORE_INCREMENT_PER_CAPTURE 2   // Amount of score increment for each hex entity capture

#define DEFAULT_TABLE_SIZE 10 // Default dimensions for both x and y of the table

#define DEFAULT_CURSOR_X 0 // Default cursor x position
#define DEFAULT_CURSOR_Y 0 // Default cursor y position